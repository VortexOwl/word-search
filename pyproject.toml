[project]
name = "word-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3",
]

[project.scripts]
start = "start:start"

[tool.uv]
package = true
